{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.1"
  },
  "scripts": {
    "dev": "npx lerna run dev --parallel",
    "build": "npx lerna run build --stream",
    "setup": "npm install",
    "start": "npx lerna run start --parallel",
    "start:frontend": "npx lerna run start --scope=@terkplumber/frontend"
  }
}
